{
    "name": "Bypass Mode",
    "cmd_t": "iot/{{ product_id }}/{{ device_id }}/properties/write",
    "cmd_tpl": "{\"properties\": {\"passMode\": {% raw %}{{ 0 if value=='Auto' else (1 if value=='Off' else (2 if value=='On')) }}{% endraw %} }}",
    "stat_t": "~passMode",
    "uniq_id": "{{ device_id }}-passMode",
    "val_tpl": "{% raw %}{{ 'Auto' if value==0 else ('Off' if value==1 else ('On' if value==2 else 'Unknown')) }}{% endraw %}",
    "stat_val_tpl": "{% raw %}{{ 'Auto' if value==0 else ('Off' if value==1 else ('On' if value==2 else 'Unknown')) }}{% endraw %}",
    "ops": ["Auto","Off","On"],
    "dev": {
      "identifiers": ["{{ device_id }}"],
      "manufacturer": "Zendure",
      "model": "Solarflow",
      "name": "Solarflow Hub",
      "sw_version": "{{ fw_version }}"
    },
    "~": "solarflow-hub/{{ device_id }}/telemetry/"
}